/**
 * Couche legacy — réexporte users / roles (Phase 3).
 */
export {
  listUsers,
  listStaffDirectory,
  createUser,
  updateUser,
  deleteUser,
  listRoles,
  listPermissions,
  createRole,
  updateRolePermissions,
  deleteRole,
} from '../api/users.api';
